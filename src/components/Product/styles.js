const styles = (theme) => ({
  root: {
    width: "100%",
  },
  container: {
    maxHeight: 440,
  },
  large: {
    width: theme.spacing(7),
    height: theme.spacing(7),
  },
});
export default styles;
